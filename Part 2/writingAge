age = int(input("What is your age? "))
if age > 5 and age < 120:
    print(f"You are {age} years old")
elif age <= 5 and age >= 0:
    print("I suppose you can't write yet")
else:
    print("That must be a mistake")
